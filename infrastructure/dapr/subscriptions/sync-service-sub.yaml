apiVersion: dapr.io/v1alpha1
kind: Subscription
metadata:
  name: sync-service-sub
  namespace: default
spec:
  topic: task-updates
  route: /dapr/subscribe
  pubsubname: pubsub-kafka
  metadata:
    rawPayload: "false"
  scopes:
  - sync-service
