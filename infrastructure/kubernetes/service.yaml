apiVersion: v1
kind: Service
metadata:
  name: todo-chatbot-service
  labels:
    app: todo-chatbot
spec:
  type: NodePort
  selector:
    app: todo-chatbot
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30080
