apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: todo-platform
  labels:
    app: audit-service
spec:
  type: ClusterIP
  selector:
    app: audit-service
  ports:
  - name: http
    port: 3004
    targetPort: 3004
    protocol: TCP
