apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: todo-platform
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
  - name: http
    port: 3002
    targetPort: 3002
    protocol: TCP
