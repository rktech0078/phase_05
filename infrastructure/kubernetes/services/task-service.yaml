apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: todo-platform
  labels:
    app: task-service
spec:
  type: ClusterIP
  selector:
    app: task-service
  ports:
  - name: http
    port: 3001
    targetPort: 3001
    protocol: TCP
